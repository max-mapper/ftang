!!!
%html{:xmlns => "http://www.w3.org/1999/xhtml", :"xml:lang" =>"en", :lang =>"en"}
  %head
    :javascript
      if((navigator.userAgent.match(/iPhone/i)) || (navigator.userAgent.match(/iPod/i)))
        {
          location.href='/iphone';
        }
    %title== FTANG!
    %link{:rel => "stylesheet", :type => "text/css", :href => "style.css"}
    %script{:type => "text/javascript", :src => "jquery-1.3.2.min.js"}
    %script{:type => "text/javascript", :src => "jqueryhoverIntent.js" }
    %script{:type => "text/javascript", :src => "jquery.listnav-2.0.js"}
    %script{:type => "text/javascript", :src => "http://www.google-analytics.com/urchin.js"}
  %body
    #header
      %h1.home_nav
        FTANG!
      #header_artist
        %span
          >
        %h1
    #content
    
:javascript
  $( function() {
    var load_artists = function() {
      $.get( '/artists', function(data) {
        $('#content').html( data );
        $('ul').listnav({showCounts: false});
        setup_routes();
      });
    };
      
    var setup_routes = function() {
      $("a").click( function(e) {
        var artist = $(this).text();
        e.preventDefault();
        $.get( $(this).attr('href'), function(data) {
          $('#content').html(data);
          $('#header_artist').show();
          $('#header_artist h1').text(artist);
        });
      }); 
    };          
    
    load_artists();
    
    $(".home_nav").click( function() {
      load_artists();
      $('#header_artist h1').text("");
      $('#header_artist').hide();
    });
    
    try {
    _uacct = "UA-9156272-1";
    urchinTracker();
    } catch(err) {}
  });